Сделаны задачи 1 и 2

---

Задача 1:

Субъективная оценка сложности задачи: 1

Предварительная оценка времени: грубвый рабочий код - 5 минут, полный полифишинг - 60 минут
Фактическое время - 67 минут

вычислительная сложность алгоритма:
Проверку на валидность не беру в расчёт, при желании можно её убрать

Один .map - это O(n)
Один .reduce - это от O(n) 
Один .map - это O(n)

Т.е. 3n

---

Задача 2:

Субъективная оценка сложности задачи: 2

Предварительная оценка времени: грубый рабочий код - 15 минут, полный полифишинг - 60-90 минут
Фактическое время - 80 минут

вычислительная сложность алгоритма:
Проверку на валидность не беру в расчёт, при желании можно её убрать

isins.map - это O(n) с коэфициентом C1
cacheResults.every - это O(n) с коэфициентом C1
cacheResults.reduce - это O(n) с коэфициентом C1
requestsResult.forEach - это O(n) с коэфициентом C2
cacheResults.map - это O(n) с коэфициентом C1

Итого 4n * C1 + n * C2, причём С2 <= C1

В тестах не хватает момента с localStorage (нужно разбираться с настройкой JEST под его эмуляцию).
И не хватает тестов на фактические запросы (т.е. не понятно как работает http.post)

---

Задача 3:

Субъективная оценка сложности задачи: 4, причём использование React + Redux для такой цели не вижу осмысленным 
(слишком маленький объем DOM чтобы использовать фреймворк)

